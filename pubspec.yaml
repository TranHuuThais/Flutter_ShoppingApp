name: shoppingapp
description: "A new Flutter project."
publish_to: "none" # Prevents accidental publication to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

ddependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - images/ # This ensures Flutter loads all images from the images/ folder
dependencies:
  curved_navigation_bar: ^1.0.6
